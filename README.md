# 🚀 GigaChat Client

Современный веб-интерфейс для взаимодействия с нейросетевыми моделями **GigaChat** и **YandexGPT**. Проект представляет собой SPA-приложение с мощным бэкендом на Node.js, реализующим продвинутую логику обработки запросов.

---

## ✨ Основные возможности

* **Мультимодельность**: Выбор между GigaChat и YandexGPT через удобный интерфейс.
* **Функциональные вызовы (Function Calling)**: Бэкенд автоматически определяет потребность в получении данных о погоде и обращается к внешним API (Weatherstack) для предоставления актуальной информации.
* **Glassmorphism UI**: Эстетичный интерфейс с эффектом матового стекла, который меняется в зависимости от выбранной темы.
* **Адаптивная тема**: Поддержка светлого и темного режимов с автоматическим сохранением предпочтений в браузере.
* **TypeScript**: Полная типизация фронтенд-части и сервисного слоя для обеспечения стабильности и удобства разработки.

---

## 🛠 Технологический стек

### Frontend
* **Framework**: Vue.js 2.6 (Class Components)
* **State Management**: Vuex (хранение истории сообщений и настроек темы)
* **Routing**: Vue Router
* **Styling**: SCSS с использованием переменных и Blur-эффектов

### Backend
* **Runtime**: Node.js
* **Framework**: Express 5.x
* **AI SDK**: GigaChat SDK

---

## 🚀 Быстрый старт

### 1. Настройка окружения
Создайте файл `.env.local` в корне проекта и добавьте следующие ключи:

```env
GIGACHAT_AUTH_KEY=ваш_ключ_авторизации_GIGACHAT
WEATHER_API_KEY=ваш_ключ_weatherstack
PORT=3000 // Опционально, по умолчанию порт 3000
```
### 2. Установка и запуск
#### Зависимости
```
npm install
```
#### Сборка
```
npm run build
```
#### Запуск сервера
```
node server.js
```
### 3. После запуска приложение будет доступно по адресу: http://localhost:3000